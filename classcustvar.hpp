#include <iostream>

using namespace std;

class mydata {

    int* ptr = new;                     // pointer to allocate memory for a single char slot
    std::string input;

    int* ptr1;
    ptr1 = &input;

public:

    mydata(int);

    void temp();

    void change(int);




};